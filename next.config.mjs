/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        formats: ['image/webp',], 
        domains: ['localhost'],  
      },

      reactStrictMode: true
};


export default nextConfig;
