/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    formats: ['image/avif', 'image/webp'],
    domains: ['localhost', 'https://672703342a603e72a78041da--unir.netlify.app/'],  
  },
  reactStrictMode: true,
};

export default nextConfig;
