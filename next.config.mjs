/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        formats: ['image/png',], 
        domains: ['localhost'],  
      },

      reactStrictMode: true
};


export default nextConfig;
